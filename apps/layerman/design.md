我想做一个框架无关的overlays管理器，它可能是dialog,modal,bottomsheet,drawer等等带半透明蒙层遮罩的对象。

这个管理器维护一份队列，依次弹出，屏幕一次只显示一个overlay,完全关闭后才会展示下一个。可以指定间隔时间

弹窗可以插队，提高展示优先级，甚至可以替换掉正在显示的弹窗(正在显示的回到队列)
弹窗可用强制显示，无视一次只显示一个的规则
弹窗可以单独配置间隔时间(delay),弹窗条件(路由，登录态，冷却时间(一天只一次，一次会话只一次，或者一天最多2次，等等，其他场景你可以补充)

api简洁直观，心智负担低
