{
  "name": "codejoo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fmt": "vp run -r fmt",
    "lint": "vp run -r lint",
    "check": "vp run -r check",
    "build": "vp run -r build",
    "ready": "pnpm run check && vp run -r test && pnpm run build",
    "dev": "vp run website#dev"
  },
  "devDependencies": {
    "@typescript/native-preview": "catalog:",
    "oxfmt": "*",
    "oxlint": "*",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.2"
}
